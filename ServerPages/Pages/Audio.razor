@using System.Threading;
@using ServerPages.Shared.Components;
@using ClientFileApi;    
@inject AudioPlayer Player;

@*<svg @onclick="@OnInitializedAsync" class="waveform" stroke="currentColor" xmlns="http://www.w3.org/2000/svg">
    <path d="@waveFormSvgData.TopPath" />
    <path d="@waveFormSvgData.BottomPath" opacity="0.5" />
</svg>*@

<MudIconButton OnClick="TogglePlayingAudioAsync" Icon="Icons.Material.Filled.PlayArrow" is-playing="@(playing is not null)" />
 


