@using System.IO;

<div class="alert alert-secondary mt-4">    
   
    <ul>
        
        @if (fileList != null)
        {
            foreach (string fileName in fileList)
            {
                <li>
                    @fileName
                </li>
            }
        }
        
    </ul>
</div>

